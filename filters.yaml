- name: demo on filters
  hosts: local
  connection: local
  vars:
    course: "DevOps with Ansible"
    myIp: "54.160.221.237"
  tasks:
  - name: greeting
    ansible.builtin.debug:
      msg: "Hi Umesh, {{ GREETING | default('Good Morning') }}"

  - name: convert to uppercase
    ansible.builtin.debug:
      msg: "{{ course | upper }}"

  - name: convert to lowercase
    ansible.builtin.debug:
      msg: "{{ course | lower }}"
      
  - name: check IPv4
    ansible.builtin.debug:
      msg: "{{ myIp | ansible.utils.ipv4 }}"