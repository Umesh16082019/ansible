- name: play level variables
  hosts: local
  connection: local
  vars_files:
  - VarsSeperate.yaml
  tasks:
  - name: print variables
    ansible.builtin.debug:
      msg: The course {{ COURSE }} is trained by {{ Trainer }} for {{ Duration }}